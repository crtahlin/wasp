# Experiment ledger

Every feature, optimization, and fork-local fix that has landed on `main`, and
what came of it. This is the human-readable index; the authoritative record is
the git history itself:

```bash
git fetch origin                     # main is a local ref; see AGENTS.md rule 13
git log --first-parent origin/main   # every experiment, one line each
git diff <merge>^1 <merge>           # exactly what one experiment changed
git log <merge>^1..<merge>^2         # how it was built
scripts/export-patch.sh <slug>       # a clean patch series against current upstream
```

Add a row when an experiment merges. Update `Result` once it has run on a real
node, including when the answer is "no measurable effect" or "made it worse".
A negative result that is recorded is worth more than one that is quietly
abandoned, because it stops the next person repeating it.

**Table: Experiments merged into wasp `main`, with outcome**

| Issue | Slug | Type | Branch | Merge | Upstream base at merge | Status | Result |
|---|---|---|---|---|---|---|---|
| [#33](https://github.com/crtahlin/wasp/issues/33) | [chequebook-chain-disabled](chequebook-chain-disabled/spec.md) | fix | `fix/33-chequebook-chain-disabled` | [`bac497c5`](https://github.com/crtahlin/wasp/commit/bac497c5) | v2.8.1 | merged | 405 rather than 500 when the chain is disabled on the available-balance path. Not a performance change, so nothing to measure; the test fails against the unfixed handler. Offer upstream. |
| [#69](https://github.com/crtahlin/wasp/issues/69) | green-tea-gc | fix | `fix/69-disable-greenteagc` | [`9702319c`](https://github.com/crtahlin/wasp/commit/9702319c) | v2.8.1 | validated | Go 1.26's default Green Tea collector corrupts the heap under wasp's allocation profile. `GOEXPERIMENT=nogreenteagc` is set in the Makefile with `:=`, so an inherited environment variable cannot silently defeat it, and CI asserts the setting is recorded in the binary. Note it is a **build-time** variable: setting it in a systemd unit does nothing, which invalidated an entire earlier measurement matrix. Affects upstream. |
| [#8](https://github.com/crtahlin/wasp/issues/8) | [sharky-concurrent-reads](sharky-concurrent-reads/spec.md) | optimization | `exp/8-sharky-concurrent-reads` | [`a583a345`](https://github.com/crtahlin/wasp/commit/a583a345), reverted by [`5392fed0`](https://github.com/crtahlin/wasp/commit/5392fed0) | v2.8.1 | reverted | In the harness, removing the per-shard read actor took Sharky from flat at ~400,000 ops/s to ~3,940,000 at concurrency 32. It was then reverted on the belief that it crashed a live node. **That was wrong**: the crashes were SIMD memory corruption (#92), and the revert was made on false evidence. Re-measured end to end after the SIMD fix: **no measurable node-level change** (1.2% faster on one comparison, 1.4% slower on another, both inside the noise). Not reinstated, because a 10x harness gain that moves nothing on a node is not yet worth the risk. #8 stays open pending #9, which the spec argues is the half that would make the ceiling reachable. A second attempt ([`14adc5f6`](https://github.com/crtahlin/wasp/commit/14adc5f6), #206) added a per-shard read/close `RWMutex` and was justified as fixing a use-after-close that had crashed the node, the same misattribution, since the node crash was SIMD (#92), and Go's own file-descriptor accounting already guards `Close` against an in-flight `ReadAt` on the real `*os.File`. It passed an 85-minute real-node soak, but the soak only shows the code is stable, not that the guard earns its place; the unguarded post-SIMD code was stable too.  Reverted by [`2ceea2c1`](https://github.com/crtahlin/wasp/commit/2ceea2c1) (#207) to restore the stopped-work state rather than keep it on a false premise. **Re-measured 2026-09-03** once #9 (`--sampler-read-concurrency`) had merged, the last missing half: on bench-1, rchash depth 9, the concurrent build at offered concurrency 64 (uncapped sharky) vs 4 (capped) gave the same range (warm ~55s, cold ~122s), with the same disk read rate (24-30k r/s) and saturation (69% warm / 94-96% cold) regardless of concurrency. The disk saturates at low concurrency, so uncapping sharky changes nothing; the constraint is disk random-read bandwidth (cold) or page-cache residency (warm), not read serialization. The 10x harness gain does not translate on a real large-reserve node even with #9. Settled negative for the reserve-larger-than-RAM regime; it would only help where RAM > reserve. |
| [#92](https://github.com/crtahlin/wasp/issues/92) | simd-scratch-stack | fix | `fix/92-scratch-stack` | [`8fe665dd`](https://github.com/crtahlin/wasp/commit/8fe665dd) | v2.8.1 | validated | The XKCP SIMD blob ran on the goroutine stack, which Go may move or grow and the collector scans. It corrupted memory and killed nodes within ~12 minutes under load. Established by A/B/A on a live node: 12 min to crash, 117 min clean, 12 min to crash. The stub now runs the blob on a pooled scratch stack, in pure Go with `CGO_ENABLED=0`. Result: **7.6 hours clean** (457 minutes, 113 peers, 305 `rchash` runs), then a further 3h46m clean over 150 runs. Both of those soaks ran the blobs as originally shipped; #90 and #91 replaced them, so that evidence does not carry to what is shipped now and a fresh soak was started on the rebuilt blobs. Do not cite the 7.6 hours as validating the current binary. `rchash` fast-cluster median 92.0s with SIMD off against 69.0s with it on and fixed, over 243 runs, **1.33x**, not the 3.16x the microbenchmark had suggested. Affects upstream. |
| [#74](https://github.com/crtahlin/wasp/issues/74) | breaker-backoff-reset | fix | `fix/74-breaker-backoff-reset` | [`22dfbdd5`](https://github.com/crtahlin/wasp/commit/22dfbdd5) | v2.8.1 | validated | The libp2p dial circuit breaker never reset its backoff after a successful dial, so a peer that failed once stayed penalised indefinitely and the backoff only ever grew. Found while investigating why a node's peer count climbed and then collapsed. Affects upstream. |
| [#85](https://github.com/crtahlin/wasp/issues/85) | breaker-never-isolates | fix | `fix/74-breaker-never-isolates` | [`2e3016b7`](https://github.com/crtahlin/wasp/commit/2e3016b7) | v2.8.1 | validated | With every known peer in backoff, the breaker refused every dial and the node could not recover on its own: a node with zero peers stayed at zero. The dial is now forced when the peer count is zero, so total isolation is always escapable. Affects upstream. |
| [#74](https://github.com/crtahlin/wasp/issues/74) | readiness-requires-peers | fix | `fix/74-readiness-requires-peers` | [`cf0fb264`](https://github.com/crtahlin/wasp/commit/cf0fb264) | v2.8.1 | validated | `/readiness` returned ready on a node with no connected peers, so an orchestrator would route traffic to a node that could not serve it. It now requires at least one connected peer. `openapi/Swarm.yaml` moved to 8.2.0 for the changed semantics. Affects upstream. |
| [#79](https://github.com/crtahlin/wasp/issues/79) | storer-shutdown-timeout | fix | `fix/79-storer-shutdown-timeout` | [`887a3352`](https://github.com/crtahlin/wasp/commit/887a3352) | v2.8.1 | merged | The storer shutdown wait was a hardcoded constant, which made a slow shutdown indistinguishable from a hung one. Now configurable, per the rule that tuning constants become configuration rather than edits (#64). |
| [#90](https://github.com/crtahlin/wasp/issues/90) | keccak-blob-rebuild | fix | `fix/90-rebuildable-keccak-blobs` | [`c950d7b6`](https://github.com/crtahlin/wasp/commit/c950d7b6) | v2.8.1 | validated | The 180 KB SIMD blobs could not be rebuilt from source on a current toolchain, so nobody could regenerate the machine code every SIMD-enabled node runs. Three causes, each pointing away from itself: gcc's default `-D_FORTIFY_SOURCE=3` made the relocation-free link impossible; the build reused stale objects, so fixing that appeared to change nothing; and `ld -r -b binary` embedded the absolute build path in the symbol names, so two builds of identical source differed. `scripts/rebuild-keccak-syso.sh` handles all three. Two builds in different directories are now byte-identical, which is what makes `CHECKSUM` mean more than "unchanged since someone pinned it". Affects upstream. |
| [#91](https://github.com/crtahlin/wasp/issues/91) | xkcp-tail-clamp | fix | `fix/91-xkcp-oob-write` | [`b5691b68`](https://github.com/crtahlin/wasp/commit/b5691b68) | v2.8.1 | validated | Both C wrappers computed a lane's final-block length as `len - max_full*136` and asserted it was 0..135. For any lane shorter than the longest (which every nil filler lane is), it went negative and wrote the padding byte before a stack buffer. Demonstrated with AddressSanitizer against the wrapper with XKCP stubbed out: overflow before, clean after. wasp does not reach it (its lanes are 64 and 96 bytes, under the 136-byte rate) and the scratch stack from #92 limits how far the damage can reach, but it is undefined behaviour in the subsystem that already killed nodes. `go_wrapper_avx512.c` carried the identical defect and was not in the report. Affects upstream. |
| [#89](https://github.com/crtahlin/wasp/issues/89) | xkcp-generated-frame | fix | `fix/89-generated-frame-too-small` | [`4b57e508`](https://github.com/crtahlin/wasp/commit/4b57e508) | v2.8.1 | merged | The XKCP generator emitted an 8192-byte Go stack frame for the 4-lane stub against a blob needing up to 11484, a ~3.3 KB overflow for anyone regenerating and copying the glue, silent because the digests stay correct. Raised to 16384, matching what the generator already emitted for the 8-lane stub. wasp does not use the generated glue at all: its stubs run the blob on a pooled scratch stack instead, so the patch protects whoever ignores that warning. Affects upstream. |
| [#99](https://github.com/crtahlin/wasp/issues/99) | replicas-cancellation-timing | fix | `fix/replicas-getter-timing-flake` | [`1e389b93`](https://github.com/crtahlin/wasp/commit/1e389b93) | v2.8.1 | validated | `TestGetter` waited 100 ms to observe an already-requested cancellation, the same order as scheduling delay on a loaded runner. It failed on three unrelated pull requests in one day while `main` was green, and passed 60 of 60 unloaded. Two mutations showed the subtest cannot detect the behaviour its name suggests, because `defer cancel()` guarantees the cancellation regardless, so raising the bound to 10 s loses no coverage. Affects upstream. |
| [#54](https://github.com/crtahlin/wasp/issues/54) | simd-by-default | optimization | `feat/54-simd-auto-detect` | [`2c205a64`](https://github.com/crtahlin/wasp/commit/2c205a64) | v2.8.1 | validated | SIMD hashing measured **25% off reserve-sample wall clock** (92.0s to 69.0s over 243 runs), corroborating the 28% projected from a 2.1x microbenchmark. Unlike #8, this one translated. It could not simply default to true, because bee refuses to start when the flag is set without AVX2, so a blanket default would stop every ARM and older x86 node booting. An unmentioned option now enables it where the CPU allows and stays off elsewhere; an explicit setting stays strict. Verified across five cases on two architectures, and confirmed in production: bench-1 logs `selected_by="cpu-detection"` with no config line. |
| [#24](https://github.com/crtahlin/wasp/issues/24) | [compaction-l0-trigger](compaction-l0-trigger/spec.md) | experiment | `exp/24-l0-depth-bench` | [`8db48cb9`](https://github.com/crtahlin/wasp/commit/8db48cb9), [`79ac8d06`](https://github.com/crtahlin/wasp/commit/79ac8d06) | v2.8.1 | neutral | The spec argued that wasp's `CompactionL0Trigger` of 8 leaves too little headroom before the write pause at 12, and proposed reverting to the LevelDB default of 4. **Measuring said the trigger is not the lever.** Both arms peak at level 0 depth 12 and pause; the trigger changes when compaction starts, not whether ingest outruns it. Reaching the stall needs about 1.98 GB/s of sustained ingest, sixteen times a 1 Gbit/s link, so no node on a normal connection can get there. #24 dropped from p0 to p2 and the sibling issue #23 raised to p0 in its place. What survives is the instrument: level 0 depth was previously collected into a histogram whose buckets ended at 10, so the number that mattered was invisible. Affects upstream. |
| [#109](https://github.com/crtahlin/wasp/issues/109) | [rpc-endpoint-failover](rpc-endpoint-failover/spec.md) | feature | `feat/109-rpc-failover` | [`d5d36cb1`](https://github.com/crtahlin/wasp/commit/d5d36cb1), [`7e853362`](https://github.com/crtahlin/wasp/commit/7e853362), [`9a4705db`](https://github.com/crtahlin/wasp/commit/9a4705db) | v2.8.1 | validated | A single RPC endpoint is what bee has, and when it fails the node does not degrade, it stops: writes fail, the postage listener stalls, and the ten-minute stall timeout shuts the node down. Measured on bench-1 with the primary endpoint blackholed: **treatment survived 15m43s and was still running when the test ended; control died at 10m01s**, which is the stall timeout to the second. Per-call routing is enough because every chain call bee makes is request/response: `transaction.Backend` embeds `backend.Geth`, sixteen methods, no subscriptions. Reads fail over; writes deliberately do not, because a lost response may mean the transaction was accepted. Affects upstream. |
| [#9](https://github.com/crtahlin/wasp/issues/9) | [sampler-io-split](sampler-io-split/spec.md) | optimization | `exp/9-sampler-io-split` | [`927db7c1`](https://github.com/crtahlin/wasp/commit/927db7c1) | v2.8.1 | merged | The sampler ran one pool doing both halves of the job, so every worker held a core while blocked on disk and the read concurrency offered to the storage layer was pinned to the core count. Now two pools: `--sampler-read-concurrency` readers and `NumCPU` hashers, with the previous `max(4, NumCPU)` as the default, so nothing changes until an operator raises it. **The 2 to 2.5x this predicted is still unmeasured**: it has to be measured together with #8 or the result is a false negative, and #8 is `blocked-on-bench`. `SampleStats.ReadConcurrency` records the value used so a measurement can say what it measured. |
| [#28](https://github.com/crtahlin/wasp/issues/28) | reserve-scan | optimization | `fix/28-batch-exists-cache` | [`3d1d3d21`](https://github.com/crtahlin/wasp/commit/3d1d3d21), [`489270ed`](https://github.com/crtahlin/wasp/commit/489270ed) | v2.8.1 | validated | `countWithinRadius` asked `batchstore.Exists` once per chunk. A reserve holds orders of magnitude more chunks than batches, so on bench-1 that was 4,064,293 lookups per wake-up against 426 distinct batches, each taking a read lock and a store `Get`. Now asked once per batch. The scan is also timed for the first time, and the number redirects the issue: **3.24 s per pass over 4,065,372 chunks**, and `IterateChunksItems` takes no reserve lock, so the premise that it is a long scan holding up other reserve operations does not survive either half. Affects upstream. |
| [#122](https://github.com/crtahlin/wasp/issues/122) | leveldb-readonly-open | fix | `fix/readonly-open` | [`1e89e4b6`](https://github.com/crtahlin/wasp/commit/1e89e4b6) | v2.8.1 | merged | A read-only open wrote the dirty-shutdown marker, which fails outright with `leveldb: read-only mode` and took the whole open down, so read-only mode was unusable. It is also meaningless: a reader neither risks an unclean shutdown nor is entitled to mark a database as in use by someone else. The dirty flag is still returned, so a read-only caller can see that a previous *writer* exited uncleanly. Affects upstream. |
| [#25](https://github.com/crtahlin/wasp/issues/25), [#26](https://github.com/crtahlin/wasp/issues/26) | sync-rate-limits | chore | `feat/25-26-sync-rate-limits` | [`cef397d9`](https://github.com/crtahlin/wasp/commit/cef397d9) | v2.8.1 | merged | The inbound chunk rate limits were constants: 250 chunks per second served per peer, 1000 accepted. Now `--pullsync-rate-limit` and `--puller-rate-limit`, with the previous values as defaults, per the rule that tuning constants become configuration rather than edits (#64). No behaviour change; what changes is that an operator can answer the question without a rebuild. |
| [#98](https://github.com/crtahlin/wasp/issues/98) | inert-gitignore | fix | `fix/98-inert-gitignore` | [`179cd601`](https://github.com/crtahlin/wasp/commit/179cd601) | v2.8.1 | merged | Patterns in `.gitignore` matched files already tracked, where they do nothing: the file is tracked, so the ignore rule never applies, and anyone reading the ignore list would conclude the opposite. Worth recording for what the CI run found rather than for the change: macOS went red on a pull request touching only `.gitignore`, and checking anyway rather than waving it through found `TestPostageAccessHandler` deadlocking for 29m15s, filed as #128. |
| [#34](https://github.com/crtahlin/wasp/issues/34) | reserve-size-one-definition | fix | `fix/34-reserve-size-two-ways` | [`7f1da3d3`](https://github.com/crtahlin/wasp/commit/7f1da3d3) | v2.8.1 | merged | Reserve size within radius was computed twice, independently: once by `countWithinRadius` feeding `/status`, and once by `DebugInfo` feeding `/debugstorage`, with different iteration bases and no shared code. Two counts of one quantity are two chances to disagree, and disagreeing about reserve size is confusing exactly when someone is debugging. The first attempt at this was wrong and the existing `TestDebugInfo` caught it: making `DebugInfo` read the maintained atomic reported **0** on a fresh store. Affects upstream. |
| [#128](https://github.com/crtahlin/wasp/issues/128) | postage-test-deadlock | fix | `fix/128-postage-test-deadlock` | [`fdcd84a6`](https://github.com/crtahlin/wasp/commit/fdcd84a6) | v2.8.1 | merged | `TestPostageAccessHandler` slept 100 ms waiting for a goroutine to enter a handler, then proceeded whether or not it had. On a loaded runner it did not, and the test deadlocked for 29m15s until the suite timeout. Replaced with a `sync.Once`-guarded channel closed from inside the mock. Found because a pull request touching only `.gitignore` went red and was checked rather than waved through. Affects upstream. |
| [#58](https://github.com/crtahlin/wasp/issues/58), [#59](https://github.com/crtahlin/wasp/issues/59) | sync-intervals | chore | `feat/58-59-sync-intervals` | [`8c71bc1b`](https://github.com/crtahlin/wasp/commit/8c71bc1b) | v2.8.1 | merged | The puller's peer recalculation interval (5 minutes) and the reserve wake-up duration (15 minutes) were constants. Now `--pullsync-recalc-interval` and `--reserve-wakeup-duration`, previous values as defaults. This is what let #18 be closed: it proposed lowering the recalculation interval to 1 minute, which now needs no code, only a measurement to justify changing the default, and that measurement needs a node syncing from cold. |
| [#11](https://github.com/crtahlin/wasp/issues/11) | [sampler-read-ordering](sampler-read-ordering/spec.md) | optimization | `exp/11-sampler-read-ordering` | [`670a8d70`](https://github.com/crtahlin/wasp/commit/670a8d70) | v2.8.1 | merged | The sampler read chunks in bin order, which comes from the chunk address and so says nothing about where the data sits in the shard files. `--sampler-sort-window` buffers a window, sorts it by `(shard, slot)` and reads in that order; **default 0, so nothing changes until measured**. The issue proposed keeping the location from the sort and reading sharky directly, which would save a lookup and is **unsafe here**: `Get` holds a per-address lock across the lookup and the read, so splitting them opens a window as wide as the sort window in which the chunk can be evicted and its slot reused, and the sampler would hash another chunk's bytes into a sample committed to a redistribution round. The location is used as a sort key only. |
| [#20](https://github.com/crtahlin/wasp/issues/20) | reserve-has-semaphore | optimization | `exp/20-reserve-has-semaphore` | [`d8484447`](https://github.com/crtahlin/wasp/commit/d8484447) | v2.8.1 | merged | pullsync calls `ReserveHas` once per offered chunk from one goroutine per syncing peer, so the concurrency the reserve sees is the peer count: a hundred peers is a hundred index lookups competing with everything else. `--reserve-has-concurrency` bounds it, default 0 meaning unbounded as before. The wait watches the store's quit channel, because `ReserveHas` has no context and a caller queued behind a full semaphore would otherwise make shutdown impossible. `bee_localstore_reserve_has_wait_duration` measures the counter-effect the issue names, since a bound set too tight trades contention for queueing. |
| [#115](https://github.com/crtahlin/wasp/issues/115) | leveldb-close-without-writing | fix | `fix/115-close-without-writing` | [`ac7f7365`](https://github.com/crtahlin/wasp/commit/ac7f7365) | v2.8.1 | merged | `Close` deleted a key, so shutting the store down required it to be writable, which is the one thing a stalled store is not. One failure became three: writes pause at the level 0 trigger, the postage stall timeout fires, shutdown blocks in `putRec`, the node is killed, and the next start reads an unclean marker it was never given the chance to clear. The marker is now a file inside the store directory, fsynced, removed only after a clean close. Inside rather than beside, so deleting the store deletes the marker; a sibling would make a freshly rebuilt store report an unclean shutdown it never had. CI caught a portability defect the local suite could not: directory fsync fails on Windows with "Access is denied". Affects upstream. |
| [#119](https://github.com/crtahlin/wasp/issues/119) | rpc-redial | fix | `fix/119-redial-dead-endpoints` | [`4c91686a`](https://github.com/crtahlin/wasp/commit/4c91686a) | v2.8.1 | merged | An endpoint unreachable at startup was dropped and never reconsidered, so an operator whose preferred endpoint happened to be down at boot ran on the standby indefinitely and had to notice and restart. Endpoints now keep their place with no connection and are dialled by `Recover`. The chain-id check moved into the dial, which closes a hole this change would otherwise have opened: startup verifies the chain once, so an endpoint adopted an hour later was never checked, and a mistyped URL that happened to be unreachable at boot would have been failed over onto. Fatal at startup, refused and logged during recovery. Refusing to start on a wrong configuration is right; killing a running node because a standby came back on the wrong chain is not. |
| [#12](https://github.com/crtahlin/wasp/issues/12) | [leveldb-block-cache](leveldb-block-cache/results.md) | experiment | `exp/12-block-cache-default` | [`09d24359`](https://github.com/crtahlin/wasp/commit/09d24359) | v2.8.1 | neutral | The 213 MB retrieval index has a 32 MB default block cache, and the issue predicted that raising it would help more on a real node than the 1.17-1.22x microbenchmark, because the index competes with 17 GB of chunk data for memory. **Measuring with the page cache dropped, so the block cache is the only index cache, said it does not help at all.** 32 MB was the fastest arm and 512 MB about 3.5 s slower; the sample's time is spent on chunk reads the block cache does not hold, and a larger cache leaves less page cache for those reads. The default stays at 32 MB, the flag stays for the rare large-reserve goleveldb node, and its cost is now documented. Not an upstream defect: the same default is confirmed reasonable. |
| [#198](https://github.com/crtahlin/wasp/issues/198) | [storage-engine-eval/followups](storage-engine-eval/followups.md) | experiment | `exp/198-pebble-followups` | [`3c96c024`](https://github.com/crtahlin/wasp/commit/3c96c024) | v2.8.1 | validated | Re-checked the three leftover Pebble follow-ups on the real node. Level 0 stays shallow over long uptime (6 files after 21 h); the eviction nudge and write-stall tail are not regimes a live node reaches. A first read of the reserve sample through `/rchash` with the page cache dropped looked like Pebble was ~3x slower, but that was a synthetic-cold artifact: **bee's own sampler runs at parity, ~40 to 48 s on both engines across real rounds**, so #185 stands. The correction is recorded in the doc and in #231, which is closed with it. Lesson: measure the sample from bee's own sampler metric, not `/rchash`+`drop_caches`. |
| [#236](https://github.com/crtahlin/wasp/issues/236) | [sampler-bmt-hashing](sampler-bmt-hashing/spec.md) | experiment | `exp/236-sampler-bmt-impl` (closed) | [`35cfb98d`](https://github.com/crtahlin/wasp/commit/35cfb98d) | v2.8.1 | neutral | On non-SIMD machines the sampler's goroutine BMT hasher spawns one goroutine per 64-byte section on top of the sampler's own chunk-level pool. A synchronous, byte-identical hash for the sampler looked like a 1.3 to 2.2x win with a third fewer allocations in an isolated microbenchmark. **On a node it did nothing:** no wall-clock gain and no allocation or GC reduction, at 8 cores or 2, SIMD off (62.2 vs 62.4 s at 8 cores; ~191 s both at 2; mallocs 991M vs 1011M per sample). The microbenchmark's tight loop kept the oversubscription at full force and forced goroutine-alloc churn that a warm-pooled node does not, and hashing is only a third of a sample's ~56 GB of allocation. Not adopted; only the record remains, like the sharky-concurrency result #8. |
| [#235](https://github.com/crtahlin/wasp/issues/235) | [reserve-size-independent-sampling](reserve-size-independent-sampling/analysis.md) | experiment | `exp/235-reserve-size-independent-sampling` | analysis-only | v2.8.1 | iceboxed | Can the reserve sample be made independent of reserve size? **Real goal (cost is O(reserveWithinRadius), ~2M-4M, a ~2x swing), but not reachable from the fork.** The linear cost is the proof of storage, not an implementation accident: the redistribution game proves reserve size by the density of the smallest-16 anchor-keyed hashes (Swarm spec Appendix C, an order statistic over the whole set). A probe-based density estimator (k anchor-derived probes, prove a held chunk near each) is a plausible sublinear replacement and is sketched, but it is a different proof shared by every node and the on-chain contract, with its own security/accuracy work; a node sampling differently loses Schelling neighbourhood consensus and fails the proof. Fork scope nil; recorded as an upstream direction, not iceboxed as wrong. |
| [#241](https://github.com/crtahlin/wasp/issues/241) | [probe-sample-cost](probe-sample-cost/results.md) | experiment | `exp/241-probe-sample-impl` | [`e15a96de`](https://github.com/crtahlin/wasp/commit/e15a96de) | v2.8.1 | validated | Measured the cost of the probe-based sublinear reserve sample sketched in #235, as a benchmark endpoint (/probesample), not a consensus change. On bench-1 over a 3.77M-chunk reserve the full sample takes ~28.5s; the probe costs ~170-200us/probe warm (~730-920us cold), so **at small k it is hundreds to over 10,000x faster** (k=16: ~10,500x warm/~1,900x cold) and still wins at every k tested (k=16384: 8.7x warm/2.4x cold). Cost is O(k), disk-dominated (index seek + chunk read; hashing is a small fraction), with a ~4-5x cold random-read penalty. Settles the cost question; the right k (accuracy calibration of the nearest-neighbour estimator) and its security are open Swarm-protocol questions, not fork work. Also fixed a deploy bug that had been silently measuring the wrong binary (cp on a running executable); the run verifies+logs the running version. |
| [#245](https://github.com/crtahlin/wasp/issues/245) | [probe-sample-k](probe-sample-k/analysis.md) | experiment | `exp/245-probe-sample-k` | [`70809924`](https://github.com/crtahlin/wasp/commit/70809924) | v2.8.1 | validated | How many probes k does the probe-based estimator (#241) need to match the current scheme's accuracy? **The same k, on the order of 16.** Both estimators are Erlang with the same shape parameter k (the current scheme's k-th smallest transformed address is a sum of k exponential inter-point gaps, Erlang(k, N+1); the probe scheme's sum of k forward-nearest gaps is Erlang(k, N)), so they have identical power to tell an honest reserve from a slacker, and only a scale constant differs, which moves the acceptance threshold, not the probe count. A standalone simulation confirms it: it reproduces the Swarm spec Table 3 at k=16 (recall error 0.098, precision error 0.072) as a validation anchor, and gives bit-identical error for both estimators at every k. Recommended range k in [16, 64]: 16 for parity with today, 32 to 64 for margin (0.03/0.02 and 0.003/0.003), still hundreds of times cheaper than the full sample per #241. Accuracy calibration only; the estimator's security (it proves a chunk within a distance, not the nearest) is a separate open question, and the sampling rule is Swarm protocol shared with the on-chain contract, not fork work. |
| [#251](https://github.com/crtahlin/wasp/issues/251) | [client-resource-curves](client-resource-curves/methodology.md) | experiment | `exp/251-client-resource-curves` | [`00f8f9ca`](https://github.com/crtahlin/wasp/commit/00f8f9ca) | v2.8.1 | merged | A repeatable methodology for measuring a Swarm client's resource use over time, at rest and during the two storage-radius events: a radius increase that evicts in a sharp burst, and a radius decrease that backfills over a long plateau. Sets out the five load sources of a storage node, the three regimes, the expected load profiles for upstream bee, wasp on goleveldb, and wasp on pebble as hypotheses to test (wasp on pebble expected flattest, because leveled compaction spreads write amplification and it stays responsive during eviction), the three ways a client could stop performing (a missed redistribution round, a write stall, read unresponsiveness), and the measurement method including a flatness score (peak over baseline, and total work over peak). Design and methodology only; the instrumentation harness and the measured curves are a later phase recorded in a results document. Not a fork change; the method is general to any Swarm client. |
| [#256](https://github.com/crtahlin/wasp/issues/256) | [stake-recovery](stake-recovery/spec.md) | feature | `exp/256a-stake-legacy-discovery`, `exp/256b-stake-recovery`, `exp/256c-stake-startup` | [`15478ecc`](https://github.com/crtahlin/wasp/commit/15478ecc), [`78335e95`](https://github.com/crtahlin/wasp/commit/78335e95), [`ea558369`](https://github.com/crtahlin/wasp/commit/ea558369) | v2.8.1 | merged | Recover a node's stake stranded in retired staking contracts (from an upstream feature request to simplify staking-contract migration). Once the network moves to a new staking contract the stake left in the old one is unreachable, because the node wires a single client at the current address and keeps no history. This adds a fork-owned catalog of historical deployments, points the existing staking client at each old address (reusing its withdraw, migrate and query methods, no new contract code), and provides: read-only discovery (`GET /stake/legacy`); explicit recovery in `withdraw` (to the wallet) or `migrate` (into the current contract) mode, with a sweep and durable idempotent per-deployment state so a part-finished migrate resumes at the redeposit without re-withdrawing; an opt-in `stake-recovery-on-startup` config (`off` default, `withdraw`, `migrate`); and graceful no-gas and no-backend handling (skip, report, retry, never block startup, never double-submit). Delivered in three phases: discovery (#258), recovery (#259), startup and no-gas (#260); spec in #257. Chain interaction only, no wire or protocol change. The catalog is seeded with the verified retired staking contracts, the two most recent retirements per chain on Gnosis and Sepolia, each confirmed on-chain to be paused and to expose `migrateStake` (#262, #264); the two oldest contracts are excluded because they predate the recovery mechanism. `stake-recovery-on-startup` is `off` by default, so recovery is opt-in per node, on demand through the `/stake/legacy` endpoints or by setting the value to `withdraw` or `migrate`. |
| [#283](https://github.com/crtahlin/wasp/issues/283) | [reserve-capacity](reserve-capacity/spec.md) | feature | `feat/283-reserve-capacity` | [`d0cf5cd2`](https://github.com/crtahlin/wasp/commit/d0cf5cd2) | v2.8.2 | merged | Make the reserve capacity configurable via a `reserve-capacity` setting (chunks), instead of the fixed `DefaultReserveCapacity` constant (1<<22). It sets the base that `reserve-capacity-doubling` multiplies, so the two compose; unset keeps the 1<<22 default, so an upgrading node is unchanged. A value below a 4096 floor is rejected and the effective capacity is logged at startup. Lets an operator size the reserve to a disk, and lets the resource-curve harness (#279) drive real radius-increase (eviction) and radius-decrease (backfill) events on an UNSTAKED node, which is otherwise impossible without staking (the doubling path is staking-gated). Local decision (how many chunks to hold, hence what storage radius), no wire or protocol change. Replaces a throwaway BEE_RESERVE_CAPACITY env var used to validate the approach. |
| [#286](https://github.com/crtahlin/wasp/issues/286) | [pebble-readamp](pebble-readamp/spec.md) | feature | `feat/286-pebble-readamp` | [`8c585ff1`](https://github.com/crtahlin/wasp/commit/8c585ff1) | v2.8.2 | merged | Expose Pebble L0 read-amplification as metrics: `pebble_level_sublevels{level}` (from `Metrics().Levels[].Sublevels`) and `pebble_read_amp` (from `Metrics().ReadAmp()`). Pebble compacts L0 on read-amplification (sublevels), not raw SST file count, so `pebble_level_files` overstates L0 depth (many non-overlapping files sit in few sublevels). Sublevels is the correct L0-depth yardstick and the one to compare against goleveldb's file count; prerequisite for a valid cross-engine L0 comparison in the resource-curve (#279) and under-load (#278) studies. Read-only gauges, no behaviour change. |
| [#273](https://github.com/crtahlin/wasp/issues/273) | [reserve-proof-mode](reserve-proof-mode/spec.md) | feature | `spec/273-reserve-proof-mode`, `feat/273-reserve-proof-mode-phase-a`, `feat/273-reserve-proof-mode-phase-b` | [`11b5915f`](https://github.com/crtahlin/wasp/commit/11b5915f), [`c8f25125`](https://github.com/crtahlin/wasp/commit/c8f25125), [`0bace397`](https://github.com/crtahlin/wasp/commit/0bace397) | v2.8.2 | validated | A config-gated experimental redistribution proof that reads only a fraction of the reserve, from the sound sublinear proof study (#271). `reserve-proof-mode` selects `classic` (default, the whole-reserve scan upstream uses and the live contract verifies, byte-for-byte unchanged) or `windowed` (a windowed order statistic: an anchor-derived sub-window of the neighbourhood, and the k smallest transformed addresses inside it, cost about N/f). The transformed address is content-bound so even spacing cannot forge it, and the window is unpredictable so a node cannot pre-pack it, which is why #271 found it sound where the bare probe scheme (#248) was not. Delivered in two phases: the sampler compute and the setting (#275), then the agent wiring that builds the commitment and inclusion proofs in the IDENTICAL, forward-compatible format (#276); spec in #274. KEY CONSTRAINT: the live Redistribution contract verifies the classic proof only. A windowed proof uses the same format but the contract rejects it, because its threshold is calibrated for the whole reserve, and clearing it is arithmetically equivalent to scanning the whole reserve. A windowed-aware contract would need only two small additions, a window-scaled threshold and a window-membership range check (the window is derivable from the anchor it already holds); that contract change is out of scope. So `windowed` wins nothing on mainnet and is off by default; it is for a testnet and research, and the node produces accepted proofs the day such a contract ships. Chain-proof semantics behind an off-by-default flag, no wire or peer-protocol change. Measured on both bench nodes (results.md), same `093d93b5` binary in each mode, five runs each, ~3.77M-chunk reserve at radius 9. A ~1/4096 window read about 900 chunks against the classic scan's ~3.77M. Within-node, config the only difference, both stores confirmed settled (sync idle, level 0 shallow): goleveldb about 36x on the median (53.6 s to 1.47 s, 47x mean), pebble about 21x on the median (33.4 s to 1.58 s, 28x mean), ~4,000x fewer chunks read on both. Pebble's settled full scan (33 s) is faster than goleveldb's (54 s), matching storage-engine-eval, which is why its multiple is smaller: the windowed proof lands at ~1.5 s on both, so a faster classic baseline means a smaller ratio. The windowed arm is deterministic (~1% run-to-run, identical hash) and yields a valid sixteen-item proof in the existing format. (An earlier draft reported pebble at 141 s / 81x from measuring before the store settled; corrected here.) Speedup is tens not thousands because phase one still walks the neighbourhood index; the direct-seek follow-up ([#277](https://github.com/crtahlin/wasp/issues/277)) would cut that floor, so these are a lower bound. The windowed sample is deterministic (~1% run-to-run, identical hash) and yields a valid sixteen-item proof in the existing format, and is far more predictable than the classic scan (which varied 52-162 s). The speedup is tens rather than thousands because the sampler still walks the neighbourhood index in phase one; a direct seek to the window's address range (follow-up [#277](https://github.com/crtahlin/wasp/issues/277)) would cut that floor too, so these figures are a lower bound. |
| [#248](https://github.com/crtahlin/wasp/issues/248) | [probe-proof-equivalence](probe-proof-equivalence/analysis.md) | experiment | `exp/248-probe-proof-equivalence` | [`e60847d0`](https://github.com/crtahlin/wasp/commit/e60847d0) | v2.8.2 | iceboxed | Does the probe-based reserve-size proof meet every requirement the current proof does? **No: it fails soundness.** It is equal on accuracy (#245), cheaper on cost (#241), equal on Schelling coordination (0.97 vs 0.97 honest-node agreement in simulation), and inherits uniformity, determinism, sybil resistance and neighborhood-depth honesty by reusing the same anchor, transform and filters, so eight of nine requirements are equivalent or better. But a slacker that stores half its share, spaced evenly across the address space, passes the probe proof 99.9% of the time (threshold set for 95% honest recall), while an honest smaller reserve at half is correctly rejected (11.8%); even spacing makes gaps small everywhere, so the unpredictable anchor does not defend against it, and the arithmetic agrees (evenly spaced m matches honest n at m = n/2). The current order-statistic proof resists this because transformed addresses are content-bound. The deeper finding is a tension: the current proof is dear because it reads the whole reserve, which is what makes it unforgeable, and the probe proof is cheap because it does not, which is what makes it forgeable, so a sound probe proof may not be a cheap one. Analysis and simulation only; Swarm protocol, not a fork change. Recorded as the reason the probe direction (#235) is not a drop-in. |
| [#271](https://github.com/crtahlin/wasp/issues/271) | [sound-sublinear-proof](sound-sublinear-proof/analysis.md) | experiment | `exp/271-sound-sublinear-proof` | [`97eeec68`](https://github.com/crtahlin/wasp/commit/97eeec68) | v2.8.2 | merged | Answers the question #248 left open, whether a reserve-size proof can be cheap without being forgeable, with a candidate that keeps both: a windowed order statistic. From the round anchor derive a window covering a fraction 1/f of the address space, its start unpredictable, and take the k-th smallest **transformed** address among the chunks inside it. The measured quantity is still the content-bound transform the node cannot place, so even spacing gives no advantage, and the window is unpredictable, so a node cannot pack chunks where the measurement looks. Cost is about N/f, sublinear. Simulation (Erlang(k,c) order statistic, n=4,000,000, k=16, 95% honest recall): the #248 even-spacing break is gone, even placement tracks random at every window size down to 1/2000 (both near 0.124 pass at half share, versus 0.999 for the broken probe scheme); a concentrating attacker at full density over a fraction of the space passes in proportion to what it stores (0.24, 0.47, 0.71 at quarter, half, three-quarter coverage), the honest gradient rather than a free pass; and a floor on cost is set by keeping the window count a healthy multiple of k, which at bench scale still buys four orders of magnitude. Verdict: a sound sublinear reserve-size proof, under an idealised model, not a protocol change or a security proof. Open problems: a formal placement-invariance bound, adaptive multi-window attacks, the on-chain contract design, and a rule for setting f from the radius. Analysis and simulation only; Swarm protocol, not a fork change. |
| [#301](https://github.com/crtahlin/wasp/issues/301) | [cheque-acceptance-cost](cheque-acceptance-cost/spec.md) | fix | `spec/301-cheque-acceptance-cost`, `fix/301-cheque-acceptance-cost` | [`bb4182ac`](https://github.com/crtahlin/wasp/commit/bb4182ac), [`a89a3a83`](https://github.com/crtahlin/wasp/commit/a89a3a83) | v2.8.2 | neutral | Take the chain calls off the path that accepts a cheque (#301, #302), found while measuring content providers (#290). A receiver asked the chain for the chequebook's issuer, its balance and its paid out total on every cheque, holding the cheque store's node-wide mutex. Measured on the bench before the change: accepting one cheque took 0.28 to 0.31 s, while a separate probe put a chain call at about 0.10 s (the results below correct that probe, which overstates what a node pays, to about 0.05 s a call), a node accepted about 3 cheques a second across all its peers, and one peer served another at about 34 chunks a second while the same download pulled about 800 across roughly 120 peers. The issuer is now read once per chequebook and kept under its own key prefix, and the balance and paid out total are reused for 30 seconds from a single reading; the signature, beneficiary and increasing payout checks are unchanged. An entry the node cannot use counts as absent, so a corrupt one cannot reject a chequebook for good. Two counters report chain reads made and avoided. Within the 30 seconds a cheque from a chequebook drained by someone else can be accepted, and one from a chequebook just topped up can be refused; the spec bounds that exposure and says it must be revisited if [#303](https://github.com/crtahlin/wasp/issues/303) and [#304](https://github.com/crtahlin/wasp/issues/304) lift the accounting gates, which matter more than this change for a single provider. Same code in unmodified upstream Bee. No wire change. **Measured** ([results](cheque-acceptance-cost/results.md)): the mechanism is proven. The node avoids about 93% of the chain reads it used to make (87% to 95% per run, 153 avoided against 12 made), and with each chequebook chain call slowed to 0.55 s a stock node took a median 1.788 s to accept a cheque against 0.362 s unslowed, a rise of about 1.4 s, which is three calls. When chain calls are slow the change is worth about 42% of that time, on thin samples stated with their sizes. When they are fast, which is what the bench had, neither the accept time nor the cheques accepted per second moved beyond the spread over six alternating cycles; only the floor moved, across 39 cheques a stock node never went below 0.184 s while a patched one reached 0.031 s in five runs of six. The spec's rule cannot be applied as written: its first figure was measured and did not move, its node-wide figure was never exercised at all, and the inference it draws from failure does not follow, since the calls are not free but cost at least about 0.05 s each here, a lower bound taken from the fastest cheque observed, and about 0.55 s each on a slow endpoint. Contention, the node-wide mutex the change is aimed at, remains unmeasured: a second paying peer contributed only about 0.2 cheques a second against a stock ceiling near 3.3, so nothing ever queued. Recorded neutral and **kept**, operator decision 2026-09-16, on resource use rather than on the gain the spec claimed. Two things were accepted: a 30 second window in which a drained chequebook can pass, itself a widening of a gap that stays open until a cheque is cashed; and, permanently, a wrong but well-formed cached issuer, which would block that chequebook for good where upstream recovers on its own. The spec's acceptance criterion was amended at the same time and marked there as amended after the measurement, with the original left visible. The node-wide gain the spec claimed remains unproven and open as [#312](https://github.com/crtahlin/wasp/issues/312). |
| [#290](https://github.com/crtahlin/wasp/issues/290) | [content-providers](content-providers/spec.md) | experiment | `exp/290-content-providers` | [`5608aa53`](https://github.com/crtahlin/wasp/commit/5608aa53) | v2.8.2 | neutral | Phase 1 of content providers, behind `providers-enable`, off by default. A node announces pinned content through `/wasp/providers` with signed records that other nodes can discover; requesters try providers first with a `wasp-local-only` stream header and fall back to normal retrieval. No wire change: the protocol freeze lock is unchanged. The [mixed-version test](content-providers/mixed-version.md) passed on bench-2: a stock v2.8.2 node ignores the header and serves or forwards as usual, every download was identical to a reference fetch, and no node blocklisted another. **Measured** ([results](content-providers/results.md)): negative on both counts, and kept rather than reverted. On speed, a hint to the provider gave 5.12 s (4.89-5.54) against 5.46 s (4.95-5.68) with no provider known, spreads overlapping, and discovery cost more than it saved on a 16 MiB file. The cause is per-peer accounting: one provider delivers only 3 to 5% of a download, the same in both settlement modes. A payment-threshold sweep moved that share to about 11% at twice the default, and to 31% and 34% over the two runs at four times it, which confirms the diagnosis but is not a setting to recommend, since a higher threshold extends more unsecured credit to every peer. On availability, which the spec made the condition for keeping the mechanism, it failed harder: after content B's postage batch expired the content was gone from the network (404 in 2.32 to 3.66 s over six runs), P still held it complete (4,194,304 bytes fetched locally with an exact hash, one manual check), discovery still found P in 0 to 3 s, and **not one of 24 runs retrieved the file** in either settlement mode. P served 36 to 57 chunks of the 1,033 in the plain copy, and no body was transferred at all. A diagnostic run shows the provider serving 43 of the 45 chunks it was asked for, and the download aborting after about 77 chunk requests because the rest are held by nobody, so the provider is never asked for most of the content it has. That run does not separate this from the credit window: the requester began 8,750,000 units in debt against an 18,000,000 threshold, about 30 chunks of headroom, so the 43 it did serve crossed that headroom and the balance plausibly sat at the threshold during the download. Both may have been at work. What can be said is that raising the credit rate alone would not have retrieved the file, because the requester stopped asking. What would happen if it persisted is not measured; 32 of those 77 requests made no preferred attempt at all and this run does not say why; and an earlier reading that blamed the credit cap alone was wrong. One diagnostic run with debug logging on, so a diagnosis to be confirmed rather than a measured result. Filed as [#313](https://github.com/crtahlin/wasp/issues/313), which is now the smaller piece of work that would make this direction testable again. **That is now measured** ([#324](https://github.com/crtahlin/wasp/issues/324), [results](content-providers/overdraft-retry-results.md)), and both were at work with the order settled. The requester stopped asking because the preferred candidate was consumed before the credit check, so a 600 ms refusal removed the only holder of a chunk for good. Persisting does retrieve more, 31.2% of the file against 6.2%, and against 0% on a first attempt after a restart, where the provider served 57 chunks and the requester could use none of them. It does not retrieve all of it, because the refusals themselves remain, which is [#327](https://github.com/crtahlin/wasp/issues/327). The spec's rule stops phase 2. Phase 1 stays on `main` behind `providers-enable`, off by default and inert when off. |
| [#303](https://github.com/crtahlin/wasp/issues/303) | [accounting-gates](accounting-gates/spec.md) | optimization | `spec/303-accounting-gates` | [`d3355b9d`](https://github.com/crtahlin/wasp/commit/d3355b9d) | v2.8.2 | merged | Spec only, no code yet, for the two accounting gates that decide how fast debt clears with one peer: #303, one payment in flight per peer, and #304, each cheque paying the debt minus the allowance about to arrive. **The arithmetic reverses what both issues claimed.** Settlement triggers at the early payment threshold, 6,750,000 units at shipped defaults, not at the 18,000,000 credit window, so the measured cheque of 6,400,000 is 95% of the trigger and there is no gap for #304 to close. More importantly, neither gate explains the measured 1.2 s cheque spacing. `paymentOngoing` cannot: `settle` adds the payment to `shadowReservedBalance` before starting it and the trigger tests the debt less that reserve, while `NotifyPaymentSent` subtracts that amount from the reserve and adds it to the balance, reducing the debt by the same amount, so the quantity the trigger tests is identical before and after the report lands, and the report took 0.3 s against a 1.2 s spacing. `refreshDue` cannot either: the timestamp it is computed from is reset whenever a refreshment completes, and the run recorded one every second, so the term was zero throughout. The spacing is simply the time to rebuild the tested quantity to the trigger while outrunning refreshment, about 12,150,000 of gross credit a cycle, roughly 40 chunks. **So the spec predicts both changes do nothing on this bench**, and is written to locate the regime where each would bind rather than to confirm a gain: #303 needs about four times the net rate at which the tested quantity climbs, which is about 2.7 times the gross rate of credit with the peer since refreshment clears a fixed 4,500,000 a second however fast traffic arrives, #304 needs a full second with no completed refreshment, where it both enlarges the cheque and, in a debt band between 4,500,000 and 5,400,000, converts a suppressed settlement into a cheque. The figures all come from one diagnostic download whose own results file says debug logging slowed it, which the spec states where they are introduced: under rule 7 that is a diagnosis, not a measurement, and reproducing it is the first condition. #303 carries a blocking design item the issue did not mention, the unlocked read-modify-write in `chequebook.Issue`, which is unreachable today and which #303 itself would make reachable ([#317](https://github.com/crtahlin/wasp/issues/317)). Reading the path also found a genuine upstream defect, the uncapped `refreshDue` at `accounting.go:484` against four capped sites ([#316](https://github.com/crtahlin/wasp/issues/316), tagged `affects-upstream`); the two changes are optimizations and are deliberately **not** tagged, which removed their rows from `docs/UPSTREAM.md`. No wire change, and adding an acknowledgement so a sender learns what a receiver accepted is ruled out of scope under rule 6. **Not yet measured**: the spec defines 30 interleaved runs across six conditions, a `payment-early-percent` sweep with a falsifiable prediction that lowering the dial raises `accounting_blocks_count`, and that counter as an acceptance input rather than only a check before starting. |
| [#324](https://github.com/crtahlin/wasp/issues/324) | [overdraft-retry](content-providers/overdraft-retry.md) | fix | `spec/324-overdraft-retry`, `fix/324-overdraft-retry` | [`061a40b3`](https://github.com/crtahlin/wasp/commit/061a40b3), [`558c93a5`](https://github.com/crtahlin/wasp/commit/558c93a5) | v2.8.2 | validated | A preferred peer refused credit for one chunk was dropped from that chunk's preferred path for good, because `retrieval.go` consumed the candidate before `prepareCredit` ran. A refusal lasting 600 ms therefore removed the only holder of a chunk permanently, and the chunk fell to peers that never held it, spent its 32 origin retries and returned `storage.ErrNotFound`, indistinguishable from content that does not exist. This is the cause behind [#313](https://github.com/crtahlin/wasp/issues/313) and it is fork-authored code, so no `affects-upstream` marker. No wire change. **Measured** ([results](content-providers/overdraft-retry-results.md)): the mechanism is proven and it is not sufficient, and both halves are recorded. Stock's first sole-source download after a restart delivers **zero bytes**, identically in two independent sessions: 59 preferred attempts, 57 hits, 9 requests refused for credit, HTTP 200 with curl exit 18. Nine refusals inside the first 262,144-byte read unit lose the whole download, because `joiner.ReadAt` is all or nothing; the provider served 57 chunks and the requester could use none of them. That is #313's own report reproduced and explained. **That comparison was not controlled and is withdrawn**: the patched arm ran immediately after three 10 MB downloads to the same provider while the stock arm ran 120 s after a restart, so the arms differed in accounting state as well as in build, against rule 7's requirement to match node state. The matched replacement, three restart cycles per build with the balance at zero at the start of each, is that **the fix delivers one read unit in one cycle of three where stock delivers nothing in three of three**, and that the fix always asks the provider more often than stock's invariant 59 attempts, at 67 to 155. The zero-byte first run is a property of cold accounting state and happens on both builds, not of the stock build as first reported. With the prefetch off the fix completes 6 runs of 6 where stock truncated 2 of 6, though only one of those runs met the condition under test on the fixed build, so that pairing is a pointer and not a result under rule 7. Content the network also holds does not regress: medians 4.30 s and 2,325,064 B/s against stock's 5.22 s and 1,917,504 B/s. A first design that waited `overDraftRefresh` for the refused peer cost about 3x on that content and was replaced by falling through to ordinary selection at once; both are kept as separate commits on the branch. **The download still truncates at the shipped lookahead buffer**, so the spec's acceptance rule is amended rather than reinterpreted: it was written as though there were one sole-source case and there are two, differing by whether the prefetch is on. Removing the permanent drop does not remove the refusal, and with the prefetch on there are 311 to 429 refusals per download, each now falling through to peers that do not hold the chunk. Falling through is what keeps ordinary downloads fast, so the two cases pull in opposite directions and the requester cannot tell which it is in. The residual is [#327](https://github.com/crtahlin/wasp/issues/327). #313 stays open. |
| [#326](https://github.com/crtahlin/wasp/issues/326) | [local-ingest](content-providers/local-ingest.md) | feature | `spec/326-local-ingest`, `feat/326-local-ingest` | [`7d108dd4`](https://github.com/crtahlin/wasp/commit/7d108dd4), [`35144644`](https://github.com/crtahlin/wasp/commit/35144644) | v2.8.2 | validated | `POST /wasp/ingest` stores content in this node's own store with no postage and without pushing anything to the network. Off by default, and with no authentication layer to put it behind the flag is the only control, which the flag help says. The count of distinct chunks is written in the same transaction that commits the pinning collection, through a fork-specific session, because `PutterSession.Done` takes only an address and a count written afterwards would be lost to a crash in between, bypassing that much of the limit permanently. The limit is a claim rather than a check, since two ingests interleave and a check against a committed total lets both pass. No wire change. **Measured** ([results](content-providers/local-ingest-results.md)): a 4,194,304-byte file ingested without postage at redundancy level NONE gives a reference **identical** to a stamped `POST /bytes` of the same bytes, on two separate files, at the predicted 1,033 chunks; a node with no hint gets 404 on three separate files; and the holder serves it **complete and SHA-256 verified** in three runs of three at a median 262,743 B/s against an interleaved control at 262,297 B/s, a difference of 0.17% with overlapping ranges, every run in both arms raising `preferred_attempts` by exactly 1,155 so both were genuinely served by the provider. The claim is **no difference detectable at this bench's resolution**, not equal speed: all six runs sit inside 0.7%. The control is content whose batch has since expired, so both arms were unstamped pinned collections and the comparison is against the pinning path rather than against live postage. Unpinning drops usage by exactly the chunk count that ingest reported, and the 507 names both the limit and what is held. **The first retrieval arm was not controlled and is reported rather than replaced**: MEDIUM redundancy against a level NONE control on a provider restarted twenty minutes earlier, HTTP 200 with 0 and 32,768 bytes returned. **The rerun changed both variables, so the cause is not established**: the failed arm also ran with `accounting_blocks_count` rising 201, 211 and 41 per run against 4, 2 and then none in the matched session, and no arm ran MEDIUM under settled conditions. What is established is the narrower negative, that the truncation is not a property of ingested content; separating the level from the accounting state belongs to [#327](https://github.com/crtahlin/wasp/issues/327). Three gaps were then closed the same night ([#341](https://github.com/crtahlin/wasp/issues/341)): **mid-stream refusal** fires on a node when the body is sent with chunked transfer encoding, confirmed by the branch's own log line and by a paired run that sent the same body the same way without the header and was refused on declared length instead, which also puts the out-of-band limit report for [#337](https://github.com/crtahlin/wasp/issues/337) through its first run outside a unit test; **neither an abandoned ingest nor a mid-stream refusal leaves chunk residue**, measured on `ChunkStore.TotalChunks` against paired control windows, every one of five windows flat at 0 where a leak would be about 512 chunks for an abandonment and 2,676 for a refusal, after three earlier attempts went wrong in three different ways, one refused on declared length so that nothing was abandoned, one asserting on the usage figure which by definition cannot see the leak, and one recording no evidence of its own; and **an encrypted ingest round-trips**, at a 64-byte reference, which is the width the record was sized for. The fourth and last of those arms is now closed too ([#341](https://github.com/crtahlin/wasp/issues/341)): **two independent nodes produce the same reference for the same bytes**, three runs, 1,033 chunks each, where what #326 had shown was one node's two write paths agreeing. Each node reads back the SHA-256 of what it received before it ingests, and a run whose SHAs do not all match is written out as refused rather than as a result, so a pass cannot come from the bytes differing. Narrow: one file size, redundancy level 0, and two of this fork's own nodes rather than a wasp node against stock Bee. The two do not run the same binary, which makes the result stronger than a first draft of it claimed, but the harness records neither version so the exact pair is not on the record. Still not shown: directories, or disk actually consumed. A review before merging found twelve defects, the worst being a usage gauge written on only one of the three paths that move it, so a node reported zero however much it held; a second review of these results withdrew the redundancy-level attribution above. The capability was then used for a sole-source size sweep that needed no expiry wait, [sole-source-sizes](content-providers/sole-source-sizes.md): retrieval completes at 10 MB and 20 MB, six runs of six, and truncates at 50 MB and 100 MB, five of five, at a flat 263,236 to 264,897 B/s whenever it completes. The cause of the truncation is NOT established there and two drafts of that document claimed it wrongly. |
| [#327](https://github.com/crtahlin/wasp/issues/327) | [per-peer-threshold](content-providers/per-peer-threshold.md) | feature | `spec/327-per-peer-threshold`, `feat/327-per-peer-threshold` | [`39bb5abb`](https://github.com/crtahlin/wasp/commit/39bb5abb), [`d94c6747`](https://github.com/crtahlin/wasp/commit/d94c6747) | v2.8.2 | neutral | A provider announces a larger payment threshold to a peer downloading content it announced, granted once per connection and never lowered, bounded by a node-wide budget. Off by default; both settings state what they cost the PEER, since an announced threshold moves that peer's early-payment point. No wire change. A review before merging found nine defects, two of which would have shipped as silent failures: Connect cleared a grant without returning its budget, so on a fast reconnect the delta was stranded for the life of the process and enough of those would stop the feature admitting anybody; and the announce serialiser returned another caller's error, which libp2p turns into a disconnect during connect. Three of its tests were vacuous. **Measured** ([results](content-providers/per-peer-threshold-results.md)): the grant fires exactly once for exactly its own size, 94,500,000, taking a fresh peer from 13,500,000 to 108,000,000 on its first provider request, and the credit is then used to the ceiling, -107,780,000 against 108,000,000. **It does not make a 20 MB sole-source download complete**: every run truncated in both arms, so the pre-registered prediction failed. The only matched pair, both arms beginning at balance zero and threshold 13,500,000, is 2,293,760 bytes against 360,448, and that is n=1 against rule 7. **The spec's motivation was also wrong**: Connect resets the announced threshold and settlement regrows it past the configurable maximum, so the feature is worth giving a FRESH peer at once what growth takes billions of units of settlement to earn, not more credit to an established one. Why downloads truncate is [#343](https://github.com/crtahlin/wasp/issues/343) and is now traced to a chunk that has stopped asking its only holder, which more credit does not prevent. |
| [#343](https://github.com/crtahlin/wasp/issues/343) | [retrieval-rate](content-providers/retrieval-rate.md) | experiment | `spec/343-retrieval-rate`, `docs/343-truncation-cause`, `docs/343-gate-terms-measured` | [`fde3cb41`](https://github.com/crtahlin/wasp/commit/fde3cb41) | v2.8.2 | open | Why provider downloads run at a quarter of the rate demonstrated possible, and why they truncate. The rate is bound by round trips rather than bandwidth: the bench pair is 30.14 ms apart and the provider reads the same file from its own disk in 0.026 s, so one in-flight chunk slot is worth about 135,900 B/s. One run at the node's own prefetch buffer moved a 4 MiB file 4.1 times faster than the baseline, SHA verified, at n=1. **Truncation is now traced** ([results](content-providers/truncation-cause.md)): the provider is refused credit for a chunk, kept for up to `maxOverdraftReadmits` of 8 further attempts by the #324 path, then dropped from that chunk; the chunk falls to ordinary selection, which for sole-source content cannot help, spends its budget of 32 raised by up to 2 on about 34 peers, and returns not-found, taking its read unit with it because `joiner.ReadAt` is all or nothing. The count of refusals that were NOT readmitted is 4 in each of two runs, matching the failures. Demotion after 16 misses is ruled out from data already held: misses rise by 1 or 2 per download. **Three explanations were proposed and withdrawn before this one**: that refusal count ends the download, refuted because the run with the fewest refusals truncated earliest; that the balance reaching the announced threshold ends it, refuted by runs at a balance of zero that delivered nothing; and a fix specified on the first of those, withdrawn when review showed its mechanism could not work, since by the point it would have waited the provider is no longer in the candidate list. Still open: why buffer 0 runs at a quarter of its own predicted rate with no credit pressure at all. The question is now **measured** ([gate-terms-measured.md](content-providers/gate-terms-measured.md)): the term that moves is the limit. Of 2,381 refusals read at the gate under its own lock, 94.5 per cent occur with `refreshDue` at zero, so the limit sits at 13,500,000 rather than 18,000,000 and every one of those would have passed at the ceiling. `refreshDue` is zero because the elapsed term is integer divided, giving nothing for the first second after a refreshment. The remedy is NOT settled: the gated sum sits against whatever limit is in force, so raising it may simply let the sum rise to meet it, and that is [#359](https://github.com/crtahlin/wasp/issues/359), tagged `affects-upstream` with both readings of the defect question laid out and delivered bytes as its acceptance criterion. A prior polling attempt ([overdraft-terms.md](content-providers/overdraft-terms.md)) reached the limit of what sampling could say and is corrected by the instrumented run. |
| [#340](https://github.com/crtahlin/wasp/issues/340) | [directory-ingest](content-providers/directory-ingest.md) | feature | `spec/340-directory-ingest`, `feat/340-directory-ingest` | [`ef00d3eb`](https://github.com/crtahlin/wasp/commit/ef00d3eb), [`d32cb541`](https://github.com/crtahlin/wasp/commit/d32cb541) | v2.8.2 | validated | `POST /wasp/ingest` with `Swarm-Collection: true` stores a tar or multipart archive as a collection with its manifest, so a directory or website can be hosted with no postage. That needed no new mechanism: `storeDir` takes a bare putter and getter and no stamper, and neither `pkg/manifest` nor `pkg/file/loadsave` references postage, so the argument [#326](https://github.com/crtahlin/wasp/issues/326) made for the blob path holds unchanged for the manifest path. File chunks, every manifest node and the root go through one counting putter on one session, so manifest chunks count against `local-ingest-limit` as they must. A collection is selected by that header **alone and never by the content type**, unlike `POST /bzz`, because dispatching on a multipart or tar content type would silently reinterpret requests that work today. The declared-length pre-flight is skipped for a collection, since it models a flat blob and knows nothing about archive framing or manifest nodes. **Measured** ([results](content-providers/directory-ingest-results.md)): **five of six arms pass and arm 4b does not meet its condition**, so `validated` here covers the arms that test this change and not the whole spec. The manifest root equals a stamped upload of the same archive on two separate archives; the holder serves the bare root through its index document and every inner path, six of six with matching hashes; a second node with no hint gets 404 in three runs; and an over-limit archive answers 507 leaving both chunk counters exactly flat in five fully qualifying runs of eight, across two passes. **Arm 4b is the exception.** A second node naming the holder is served every single-chunk **inner** path, twelve of twelve, but the collection's one multi-chunk file returned **HTTP 200 with an empty body in two of thirteen attempts**, which the spec's "in all three runs" does not allow. Inside the gated pass alone, where nothing else differs, three single-chunk paths were correct nine of nine against the multi-chunk file at one of three. That is the signature [#313](https://github.com/crtahlin/wasp/issues/313) reports and [#343](https://github.com/crtahlin/wasp/issues/343) traced to `joiner.ReadAt` losing a whole read unit when one chunk is refused credit and falls to peers that never held it; both are open, and the attribution is consistent-with rather than established. Two facts put the failure downstream of everything this change added: a 200 with an empty body means the response headers were already written, so the manifest had resolved and the path lookup had succeeded, where a manifest fault gives 404 before headers; and the holder itself served a ten-chunk file, `/img/pixel.bin` at 40,960 bytes, with a matching hash, so multi-chunk resolution through a manifest works and the variable is chunk count **over the remote hinted path**. What this pass adds to #313 is that contrast, which it lacked. #340 is not reopened, because the retrieval path is not what this change touches. Separately the bare root is intermittent at first contact, **one failure in twelve tabulated attempts**, with a second in a pass whose rows were set aside for a harness fault and which is therefore not counted; both were the first request in a sequence with no connectivity gate, most likely because `Wasp-Providers` connects asynchronously and a preferred candidate must already be connected, which is also correlational and not settled. **Two harness faults are recorded with the results**, because the second one nearly published a false accept: one output file for every path with no size printed, so a fetch reported the previous file's hash; and scoring a fetch on the HTTP status code, which counted a zero-byte 200 as a success and is rule 7 being broken inside the tool written to enforce it. **The provider was first found running the #326-era build, which ignores the header entirely**, and arm 1 failed against it: proved rather than assumed, because the same tar with and without the header returned the identical reference. **Two spec defects, both found only by running it.** Arm 5 compared the reported count against the `TotalChunks` rise and demanded equality, which a collection can never satisfy: measured, the count is exact against `ReferenceCount` every time and short against `TotalChunks` by 3, being manifest chunks the node already holds, so as merged the rule would have rejected correct code. And arm 1 destroys arm 4's precondition, because its stamped upload publishes the content under the same reference the 404 arm needs to be unreachable, which it did. **A review then found two more, one of them in the correction above.** Arm 6's residue check had **no power at all as first run**: its own first run ingested the archive successfully, so the later refusals re-posted content the node already held, and `TotalChunks` cannot rise for a chunk already stored, meaning it reads flat on a leak exactly as on a clean refusal. Re-run with a never-ingested archive per attempt and `ReferenceCount` read beside `TotalChunks`, it gives eight 507s across two passes with `held` unchanged within each pass and five runs at exactly zero against exactly flat windows, with three sensitivity runs on a fitting archive moving both counters to show the check can see an ingest at all. `held` alone would not have shown it: `committed` is raised only on the completed path, so a refusal cannot move it however much it leaks, which is why the arm reads database-wide counters that owe nothing to the code under test. And the corrected arm 5 rule demanded equality **exactly**, which a 25-second ingest refuted: reported 303 against a `ReferenceCount` rise of 309, where `TotalChunks` at 306 cannot exceed the reported count on its own, so the excess is drift; solving the three readings gives drift 6 and 3 pre-held chunks, and the same archive ingested in 0.11 seconds gives 303 against 303 exactly, three times. The cause is the **measurement window**, not the ingest: the two harnesses differ by a five-second sleep between the ingest and the second counter read, and the ingest itself took 0.11 seconds in both. So exactness is a property of a tight window rather than of a quiet node, and the spec now requires the tight window and treats a single differing run as one to repeat rather than as a result, because a control window reading zero did not predict a measurement window drifting by six. The shortfall reads 3 at 26, 206 and 303 chunks and 0 for a blob, which the mechanism requires since a blob has no manifest; a derived 9 at about 5,841 chunks is **uninformative** rather than contrary, being equally what a shortfall of 3 gives under a drift of minus 6, and an earlier version of this row treated it as a refutation. All four rules are corrected. Three review rounds preceded the merge and the third was the useful one: it found the code correct and the tests weak. One test was provably vacuous, passing the shared helper's boundary as a content type so the multipart branch it guarded was never exercised; the leaked-collection risk the spec names had no test at all, and covering it needed an archive that fails only after chunks are stored, since one bad from the first byte strands no claim; and the two tests that ask the node to serve a path, the only ones that need the feature to work, had been omitted without mention. Fixing them surfaced a real defect: a truncated archive answered 500, because a body under 512 bytes gives `io.ErrUnexpectedEOF` rather than `tar.ErrHeader`. Both are the caller's fault and both now answer 400. Also corrected: the counting test compared against the file count of three when the files alone are six chunks, so a putter skipping every manifest node would have passed it. An encrypted collection is **not** deterministic, since every chunk takes a fresh key, so address equivalence cannot hold for one and the duplicate path can never fire; and an existing pin at that root, whatever created it, shadows an ingest of the same content. The manifest root is host-scoped, not a pure function of the bytes: for a tar the entry content type comes from the host MIME table, so two nodes on different distributions can produce different roots, which is recorded as a cross-host question and not claimed. Cost is about six chunks per small file, about 10,300 files against the shipped limit. No wire change. |
| [#353](https://github.com/crtahlin/wasp/issues/353) | [overdraft-observability](content-providers/overdraft-observability.md) | feature | `spec/353-overdraft-observability`, `feat/353-overdraft-observability` | [`66610224`](https://github.com/crtahlin/wasp/commit/66610224), [`5e527a22`](https://github.com/crtahlin/wasp/commit/5e527a22) | v2.8.2 | validated | An overdraft refusal was invisible: `PrepareCredit` returned `ErrOverdraft` after incrementing one unlabelled counter and wrote no log line at any level, so a chunk refused for credit and a chunk that does not exist both still surface as `storage.ErrNotFound`, since the change adds a line and does not touch the returned error. The lock-contention path beside it does log, so lock contention was diagnosable and credit refusal was not. Adds one V(2) line at the refusal carrying the peer, the price, both sides of the comparison, and the terms feeding them: the settled balance, surplus, reserved and shadow reserved balances, the raw refresh timestamp and saturated elapsed time, and whether the settle branch was entered. That elapsed field was `elapsed_seconds` and is `elapsed_ms` from [#359](https://github.com/crtahlin/wasp/issues/359), because a whole second reads zero across the window continuous accrual grants the allowance in. The refresh rate itself is not logged, so the limit is reconstructable from the line but not every multiplicand of it. Built once in `NewAccounting` rather than per call, because `logger.V(2).Register()` forces a full `Build()` whether or not the level is enabled and `PrepareCredit` runs once per chunk per peer attempt. Building it there is also what makes the level raisable **before** the node has carried traffic: the other eight V(2) registrations in the file are lazy, and raising verbosity while only a V(0) entry exists sets that entry to 0, which a V(2) child then inherits, so the line stays silent. Guarded by an explicit verbosity check, since the arguments and a state-store read are otherwise evaluated at any level. No config, no wire change, and no behaviour change, the last asserted by a test rather than claimed. **Validated** by the measurement it existed to unblock ([#343](https://github.com/crtahlin/wasp/issues/343), [gate-terms-measured](content-providers/gate-terms-measured.md)): 2,381 refusals read under the same lock as the comparison, every figure in that document reproducing from the raw capture, which settled a question that had cost four withdrawn designs and two withdrawn analyses. The spec took five revisions and four reviews; its two worst defects were reporting an empty diff from a stale `main` as verification, which became [#355](https://github.com/crtahlin/wasp/issues/355), and a fabricated citation pointing at a test line as though it already carried the argument that makes it work. The implementation review found seven more, of which the most instructive was that the justification for capturing the post-settle balance was vacuous in three places at once, the code comment, the merged spec and a commit message claiming a test covered it, when the peer lock excludes the concurrency all three invoked. A dead assertion and a test that could not fail were found in the same pass. |
| [#369](https://github.com/crtahlin/wasp/issues/369) | [discovery-lifetime](content-providers/discovery-lifetime.md), [results](content-providers/discovery-lifetime-results.md) | fix | `spec/369-discovery-lifetime`, `fix/369-discovery-lifetime` | [`f74f0234`](https://github.com/crtahlin/wasp/commit/f74f0234), [`d48b11e5`](https://github.com/crtahlin/wasp/commit/d48b11e5) | v2.8.2 | merged | Provider discovery and hinted connection are node-scoped background work, but both derived their context from the HTTP request that happened to start them, so both were cancelled when that request ended. Measured before the change: the lookup is cancelled **3 of 3** at the default redundancy level, and at level NONE it completes and the **dial** is cancelled instead, 3 of 3, on the same bytes uploaded twice with the same batch. So erasure coding moves the cancellation from the end of the download to the first few milliseconds rather than creating it, which corrects the scope the issue was filed with, and **fixing only the context the trigger passes would move the failure from the lookup to the dial rather than remove it**. Both now derive from `context.WithoutCancel`, bounded by a 30 second `discoverTimeout` and still ended by the service context. The trigger is untouched deliberately: it cannot tell whether it holds a reader context or a per-shard prefetch context, so the decision belongs where the lifetime is known. The lookup cache also never warmed, because `Lookup` refuses to cache an interrupted result, so the cost was paid on every download and the benefit never kept. **Ships with eight counters in a new `pkg/providers/metrics.go`**, because the package had no metrics and no success logging at all, which is why the first three versions of the spec each contained arms whose null result was byte-identical to their positive one. **Arm 1 is measured and passes; arms 2 and 6 were run and neither can be settled on this bench; arms 3, 4 and 5 were not run**, which is why this row stays `merged` rather than `validated` and the issue stays open. Measured ([results](content-providers/discovery-lifetime-results.md)): the lookup completes **6 of 6**, three runs at the shipped default redundancy level and three at level NONE, against 3 of 3 cancelled at the default level before the change. `lookups_canceled` stays flat at zero throughout, `lookups_completed` and `connects_dialed` rise by exactly one per run, and `lookups_served_from_cache` stays at zero, which is the guard that every run did real work rather than being answered from the ten-minute cache. **Arm 1 does not test the lifetime claim**: the dial completing inside the request would satisfy it equally, and at the default level the spec predicts exactly that. Arms 2 and 6 are what test it. This row was written before they ran and said neither was run; **both have since been run, and neither can be settled on this bench**, for reasons recorded in their own sections of the results document. Arm 2's three runs are unresolved rather than negative: one failed only the ordering requirement that has since been withdrawn, and in the other two both events landed in the same sampling bucket, which cannot be ordered at all. The before state comes from a different session and a different observable, a log line rather than these counters, which is a deviation from the spec argued there on the ground that arm 1 reads a structural outcome rather than wall time. **The alternating design was abandoned for a measured reason**: every restart of the requester leaves the local store dirty and costs about six minutes of sharky recovery, measured as still not ready with zero peers after five and a half minutes on a store of 2.5 million chunks, so six restarts per run costs about half an hour of recovery. That is a cost rather than a risk, these being experiment nodes whose stores are not precious, and an earlier version of this row called it a risk; arm 4 needs the alternating design and is worth the time, while arm 1 is exempt only because its observable is structural. **Three harness faults, each caught by a gate that did not exist at first**: a postage batch the provider no longer had, which gave twelve empty references and twelve 404s in 0.01 seconds; no gate on the first upload, so twelve uploads and twelve downloads ran without anyone checking the first one worked; and waiting on `/health` rather than `/readiness`, which downloaded against a node still in recovery and gave ten 503s in 0.01 seconds with the counters reading empty. The spec pre-registers a **negative**: the download that triggers discovery is predicted not to get faster, because a lookup takes a median 1.67 s and the dial was still running after 3.5 more, while chunk 64 arrives early in a download ending about five seconds later. Any value is in later downloads of the same content, which is its own arm and may come back negative. It also resolves what #369 records as a reason to settle it together with [#299](https://github.com/crtahlin/wasp/issues/299): the two do not overlap, because #299's own design argues `Discover` reads through the plain storer getter rather than the download's wrapped one, so #299 was unblocked by the spec merging rather than by this code landing. **Four defects in the spec's own acceptance list, each found by running it rather than reading it**, and the history is kept in the spec because the pattern is the point: three arms that could not fail, watching a log line that does not exist or a connection hive forms anyway; then a condition **unsatisfiable on correct code**, introduced while fixing them, `LookupsCompleted` being defined so a cache hit raised it too; then the same dial-attribution confound surviving two further fixes, from `/peers` to a counter to a split counter, because libp2p's already-connected short-circuit is keyed on the remote **address** rather than the peer; then kademlia re-dialling inside the measurement interval satisfying even a three-leg conjunction. What was believed to hold was changing the shape of the criterion rather than its subject, to an **ordered** conjunction, and the spec says plainly that even that is inference rather than proof. **That ordering requirement has since been withdrawn as unsatisfiable, which makes it the fifth defect in the same acceptance list rather than the fix for the other four.** It required the overlay's first appearance in `/peers` to be at or after the second `ConnectsDialed` rose, but the registry entry is written by `addIfNotExists` inside `Connect` while the counter is incremented after `Connect` returns, so a genuine discovery dial always writes the overlay first and the requirement is met only by a sampling coincidence. Found while writing [#382](https://github.com/crtahlin/wasp/issues/382); the withdrawal is recorded in the spec and the results document. **Three review rounds on the code found four more**, every one a test that passed for the wrong reason. The first version of the unit tests passed against the unfixed code, because the in-memory network takes `_ context.Context` and the stub ignored it too. `go test -race` failed **every** test in the package, including ones that predated the branch, because the test hook for the timeout was a package variable one parallel test wrote while others read it. A stated acceptance condition was not verified at all: with the shutdown hookup deleted the stops-on-close test still passed whenever the parallel timeout test had shortened that variable. And the denominator test tested only that the counter rises when everything works, where the property is that it rises for a run that found **nothing**; the obvious replacement, three runs across the cache window, does not catch that either, because every lookup in it finds records. Windows CI then caught a fifth that local runs, the race detector and three reviews all passed: a test whose correctness depended on a one-nanosecond timer winning a race, fixed with an already-expired deadline, which `context.WithDeadline` cancels synchronously. **The suite is now checked by eleven mutations, all caught**, rather than by reading it. Also corrected along the way: `Options.Connect` reports an already-connected peer in a `bool` return rather than a sentinel error, since a success signalled by a non-nil error is a trap for the next caller; neither connect loop checked its context, so one shutdown counted a failure for every remaining record and would have made `ConnectsFailed` useless as evidence that providers are undialable; and the 15 second dial timeout in p2p is per **underlay** rather than per overlay, with up to four per record, so one `Connect` can consume 60 seconds and the "about two overlays" figure holds only where each address has one underlay. No wire change, and no `affects-upstream`: `pkg/providers` does not exist upstream. |
| [#299](https://github.com/crtahlin/wasp/issues/299) | [erasure-preferred](content-providers/erasure-preferred.md) | fix | `spec/299-erasure-preferred`, `fix/299-erasure-preferred` | [`5f16982a`](https://github.com/crtahlin/wasp/commit/5f16982a), [`48f02078`](https://github.com/crtahlin/wasp/commit/48f02078) | v2.8.2 | merged | The preferred set did not reach the chunk fetches an erasure-coded download actually makes. The redundancy decoder's prefetch builds its context from `context.Background()`, which carries no values, so providers named by a hint or found by a lookup were not tried for those chunks: `PreferredPeers` returned nil and the fetch went straight to ordinary peer selection. **It is a race rather than a total loss**, decided by a compare-and-swap, and the spec refuses to say how often the prefetch wins because no run has counted it. The set is re-attached in this fork's own getter wrapper, the last place before retrieval where fork code runs, which keeps **`pkg/file/` byte-identical to upstream**, verified rather than assumed: changing it would have meant altering an exported struct or a constructor signature there and carrying that divergence through every future sync. **Ships with `bee_retrieval_preferred_candidates_selected`**, counted once per flight whose candidate list is not empty, because no existing counter can answer the question: `preferred_attempts` increments only after `prepareCredit` succeeds and so is capped by credit rather than by whether the set was present, and attempts plus overdrafts is inflated by the #324 readmit path, which keeps a peer without consuming the candidate, so one refusal can be counted nine times. **Not measured on a node yet**, which is why this row is `merged` rather than `validated` and the issue stays open; the spec pre-registers that the arms may come back negative, since the value depends on the providers being connected and creditable. **The cost is recorded beside the benefit**: reservations that were spread across the neighbourhood now concentrate on the few peers in the set, nothing bounds how many of those fetches run at once, up to 119 shards per live decoder at the default upload level, and driving refusals on one chunk past the readmit bound of 8 makes truncation more likely. **Three review rounds, and the useful finding was about the tests rather than the code.** The counter's headline property, once per flight, had no test at all: every subtest drove one attempt in one flight, so moving the increment into the per-attempt branch or hoisting it out of the singleflight closure both passed, and the mutation table in the first commit message avoided exactly those two. Building the missing test took two attempts, because a second preferred attempt does not happen when the first peer lacks the chunk, a miss returns quickly and the flight falls to ordinary selection, but when the first attempt starts and is then slower than `preferredWait`; and because candidates are sorted by closeness to the chunk rather than by the order they were added, so one fast peer let the flight finish in a single attempt. Four mutations are now checked and all caught. **Four documentation claims were also wrong, two of them the opposite of the code**: that reservations concentrate up to a concurrency limit, where there is none; that a peer which errored on a chunk in the last minute is excluded from the counter, where a miss at a preferred peer is deliberately not a peer error, which is the point of the local-only design; that the concentration is onto one provider rather than the few in the set; and a cost stated flatly while the benefit beside it carried an unmeasured hedge, which rule 7 applies to both directions. No wire change. |
| [#382](https://github.com/crtahlin/wasp/issues/382) | [already-connected](content-providers/already-connected.md) | fix | `spec/382-already-connected`, `fix/382-provider-connect-counted` | [`69e3d643`](https://github.com/crtahlin/wasp/commit/69e3d643), [`9e6552fc`](https://github.com/crtahlin/wasp/commit/9e6552fc) | v2.8.2 | merged | `bee_providers_connects_dialed` and `connects_already_connected` were split on `p2p.ErrAlreadyConnected`, which is decided by matching the remote **address** of an open connection rather than the peer, so a provider already connected on a different underlay returned a plain success and was counted as a dial. A provider record's addresses are liable to differ from the address book's, since the record is filtered to public underlays. **The fix is in the fork's own adapter and changes no behavior**: the peer set is read immediately before the connect and used to classify it, while the connect keeps the same call, error paths, overlay guard and topology notification, in the same order, verified line by line. **The counters over-report dials and now say so**, because the peer set lists peers whose handshake has finished while a connect needs no dial as soon as a transport connection exists, so a peer connecting concurrently is counted as a dial for the length of that setup; an earlier version of this change called that window "two adjacent statements", which understated it in the direction that flattered the design. **The address keying itself is deliberately not fixed**, because doing so is three behavior changes rather than one: kademlia stops announcing a peer it re-dials, this adapter stops notifying topology on every provider connect to an already-connected peer, and `POST /connect/{multiaddr}` turns from 200 into 500 for a peer connected on another underlay, on a path no test in `pkg/api` covers. Against one saved handshake per occurrence that needs its own issue, so #382 stays open with `affects-upstream` as the marker. **Thirteen mutations checked and all caught**, including a short circuit that returns early on an already-connected peer, which passed every arm as first written while skipping both the connect and the topology notification; the arm that rejects it asserts both were invoked rather than only the returned value. A unit test in `connections_test.go` pins **current** libp2p behavior rather than the fix, so changing the keying has to change a test, and it is verified by making the check peer-keyed, which fails it while both neighbouring tests pass. **No measurement, and none is possible on this bench**: the counters are node-wide and unlabelled, the provider connect outlives the request, the two nodes reconnect faster than a request ends, and the dial usually completes inside the request. Four bench arms in the first draft of the spec were defeated by those four facts, which is why this row is `merged` rather than `validated`. **The evidence the issue was filed on was withdrawn.** It read a bench run in which the provider's overlay appeared in `/peers` at 1.2 s and `connects_dialed` rose at 1.4 s as proof that an already-connected peer had been counted as a dial. The ordering is fixed by the code in the opposite direction: the registry entry is written by `addIfNotExists` **inside** `Connect` while the counter increments **after** it returns, so that is the signature of the node's own successful dial, and `addIfNotExists` also has an inbound caller with no `Connect` on the stack, so the ordering excludes nothing either. The run is ambiguous. **A merged #369 acceptance criterion fell with it**: arm 2 required the overlay's first appearance to be at or after the second the counter rose, which a genuine discovery dial can satisfy only by a sampling coincidence, and that was the sole reason arm 2 run 1 was recorded as a failure. Withdrawn, leaving arm 2 unresolved rather than negative, and the #369 ledger row corrected where it said the ordered conjunction finally held and that arm 2 had not been run. **Five review rounds, and every one found more in the claims than in the code**, including two rounds whose findings were errors introduced by the previous round's corrections: a retraction that repeated the error it retracted, a timing claim of "microseconds apart" wrong by orders of magnitude, an assertion that arm 6 had not been run when the results document has a section on it, and operator-visible Prometheus help strings left describing the behavior the change removes. The last round's fixes were made by deleting overclaims rather than writing new argument, because new prose is where the errors kept appearing. Also found on the way: [#387](https://github.com/crtahlin/wasp/issues/387), a postage test that fails about one run in 256 because it assumes two random addresses land in different buckets, which turned this branch's CI red while touching nothing it changes. No wire change, and no `affects-upstream` on this change: `pkg/node/providers.go` is fork-authored and has no upstream counterpart. |
| [#359](https://github.com/crtahlin/wasp/issues/359) | [continuous-accrual](content-providers/continuous-accrual.md), [results](content-providers/continuous-accrual-results.md) | optimization | `spec/359-continuous-accrual`, `exp/359-continuous-accrual` | [`2268503b`](https://github.com/crtahlin/wasp/commit/2268503b), [`54370d27`](https://github.com/crtahlin/wasp/commit/54370d27) | v2.8.2 | neutral | The allowance that raises a peer's overdraft limit above the threshold it announced is `min(elapsed/1000, 1) * refreshRate`, integer division, so it is zero for 999 ms and then a whole `refreshRate`, although `refreshRate` is documented as a rate. [#353](https://github.com/crtahlin/wasp/issues/353) measured **2,381 refusals** and **94.5 per cent fell in the window where the term reads zero**. `refresh-allowance-accrual: continuous` accrues per millisecond instead. **Off by default, and measured** ([results](content-providers/continuous-accrual-results.md)): fourteen downloads on the bench. **The mechanism works at exactly the predicted values and the safety hazard did not occur**: the provider never blocklisted the requester, `disconnects_overdraw_count` stayed at zero, the cap bound at exactly 3,374,999 giving an overdraft limit of exactly 16,874,999, and refusals at the bare announced threshold went from **461 in one download to zero**. Overdrafts not readmitted fell from a median of 150 to **0**. **It delivered no additional bytes**, because all twelve runs completed in both arms, so the row is `neutral` rather than `validated`. **The control condition the acceptance table assumes did not reproduce**: the table is built on a control that truncates, citing 1 of 3 completing at this lookahead, and the control completed 3 of 3 with overdrafts not readmitted at 217, 150 and 50 rather than the stated baseline of about 4. No outcome in the table describes what happened. Why the baseline moved is **not established**, and attributing it to #324, #299 or the fresh content would be the same unsupported causal reading this repository has already retracted twice. **One observable was discarded as an artifact before it could be reported**: the floor and ceiling refusal split is defined on `refresh_due` being exactly zero, which the step model produces constantly and the continuous model almost never does, so its collapse from thousands to zero is true by construction and says nothing; the overdraft limit at which refusals actually happen is the comparable quantity and is what the 461 to zero figure above rests on. **The cap is the weak point, not the arithmetic**: a peer announces a threshold but not its tolerance, so this node's own stands in for it, less one unit because the peer disconnects at equality while this admits it, giving 16,874,999 against the 16,875,000 at which a stock peer disconnects. The cap stops applying at one second, or it would hold the limit below what the peer already tolerates forever, and a failed refreshment leaves the timestamp at zero and lands in exactly that branch. **Two things that were not the headline.** A backwards clock step made the term negative and put the limit **below** the announced threshold, which changes what a node with no configuration does and is tagged as an upstream defect in its own right. And `PeerAccounting` was a second copy of the same expression, so `/accounting` could publish a limit disagreeing with the one enforced; that is the shape [#316](https://github.com/crtahlin/wasp/issues/316) already records for the third copy in `settle`. The refusal log line's `elapsed_seconds` becomes `elapsed_ms`, since a whole second reads zero across the window the allowance is granted in. **Ten mutations checked and all caught, after review found the change had no test where it changes anything**: reverting the one line in `PrepareCredit` made `continuous` a no-op at the gate while the whole package still passed, because every test reached the helper directly or through the reporting path, and the commit message had claimed eight mutations all caught with that one omitted. The same review found the backwards-clock claim wrong in four places including the merged spec: a full second is needed only in the step arm, while the continuous arm goes negative at **one millisecond**, which is the ordinary size of a clock correction, so the case described as harmless was the reachable one. It also found the node wiring untested, so deleting `SetAccrualMode` left the setting parsed, validated, logged and inert; and three documentation gaps, including a `config-reference.yaml` that asserts it lists every option and did not list this one, and `payment-tolerance-percent` silently acquiring a second meaning, since under `continuous` it also bounds what this node will overdraw against a peer. No wire change. |

Status values: `merged` (in `main`, not yet validated on a node), `validated`
(measured on the bench, effect confirmed), `neutral` (no measurable effect,
kept or reverted; say which), `reverted` (backed out, with the reason),
`superseded` (upstream or a later experiment replaced it).

## Worked example

For the first entry above, each claim in this table is checkable:

```bash
git diff bac497c5^1 bac497c5      # exactly what the experiment changed: 2 files, 60 lines
git log bac497c5^1..bac497c5^2    # how it was built
scripts/export-patch.sh chequebook-chain-disabled
```

The last of those replays the experiment onto **current** upstream master and
writes a patch series to `dist/patches/`. It worked against upstream `a93fb7a7`,
which is already newer than this fork's `v2.8.1` base, which is the point: the
series is reconstructed from the merge, not stored, so it does not go stale.
