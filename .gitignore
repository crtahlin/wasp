/dist
/.idea
/.vscode
/tmp
/vendor

# Compiled Object files, Static and Dynamic libs (Shared Objects)
*.o
*.a
*.so

# Folders
_obj
_test

# Architecture specific extensions/prefixes
*.[568vq]
[568vq].out

*.cgo1.go
*.cgo2.c
_cgo_defun.c
_cgo_gotypes.go
_cgo_export.*

_testmain.go

*.exe
*.test
*.prof

.DS_Store


# `go build ./cmd/bee/` (no -o) writes the binary here. `make binary` uses
# dist/, which is already ignored, but a bare go build is easy to run by
# accident and a ~70MB blob is easy to sweep in with `git add -A`.
/bee

# Agent worktrees and local session state. A sync script committed a
# gitlink from here once; keep the whole directory out.
.claude/
